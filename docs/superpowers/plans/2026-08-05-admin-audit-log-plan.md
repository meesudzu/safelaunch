# ADMIN-01 — Audit log viewer implementation plan

1. Add Worker tests for mapping, filters, validation, and cursor pagination.
2. Implement `GET /v1/admin/audit` with bounded keyset pagination.
3. Add typed Web API client support and its request contract test.
4. Add `/admin/audit` with Vietnamese filters, table, states, and pagination.
5. Run focused tests, lint, typecheck, and builds; update task status.
