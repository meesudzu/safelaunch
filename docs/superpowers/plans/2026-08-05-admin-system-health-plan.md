# ADMIN-05 — System health implementation plan

1. Add tests for independent dependency failures and aggregate-only output.
2. Implement D1, R2, and Vectorize probes with per-section status and timestamp.
3. Represent Queue, Workflow, DO, and Workers AI as explicit unknown sections.
4. Add `/admin/health` and navigation with accessible status text.
5. Run tests, privacy checks, lint, typecheck, and builds; update the backlog.
